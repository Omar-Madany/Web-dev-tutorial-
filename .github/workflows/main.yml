name: Deploy Recipes

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Get repository
        uses: actions/checkout@v6

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Recipes
        uses: actions/upload-pages-artifact@v4
        with:
          path: "./Web Dev Essentials/HTML Essentials/odin-recipes"

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
